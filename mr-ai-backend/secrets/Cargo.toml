[package]
name = "secrets"
version = "0.1.0"
edition = "2024"
license = "FSL-1.1"
description = "Pluggable secret provider (env / mounted files) used by mr-ai-backend."

[lib]
name = "secrets"
path = "src/lib.rs"

[dependencies]
async-trait = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
domain = { workspace = true }

[dev-dependencies]
tempfile = "3"
